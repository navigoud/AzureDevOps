apiVersion: apps/v1
kind: Deployment
metadata:
  name: docker
  namespace: arun
  labels:
    app: arun
spec:
  replicas: 3
  selector:
    matchLabels:
      app: arun
  template:
    metadata:
      labels:
        app: arun
    spec:
      containers:
      - name: dotnet2
        image: nbyreddy/dockerdep-10-app:1.0.0
        ports:
        - containerPort: 80